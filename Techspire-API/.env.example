# Techspire Backend Environment Variables

# MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/techspire

# JWT secret for token signing (change in production!)
JWT_SECRET=your_jwt_secret_here

# Server port
PORT=3000

# Frontend URL for CORS (if not using Vite proxy)
FRONTEND_URL=http://localhost:5173

# Environment: development | production
NODE_ENV=development

# Admin seed script (used by scripts/seed-admin.js)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=  (optional; if omitted, a random password is generated)
